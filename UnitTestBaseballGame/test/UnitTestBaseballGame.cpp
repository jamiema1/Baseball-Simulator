#include "pch.h"
#include "CppUnitTest.h"
#include "/dev/Code/BaseballGame/BaseballGame/src/main/player.h"

using namespace Microsoft::VisualStudio::CppUnitTestFramework;

namespace UnitTestBaseballGame
{
	TEST_CLASS(UnitTestBaseballGame)
	{
	public:
		Player* p; 
		
		TEST_METHOD(TestMethod1)
		{
			//p->Player::printFullName();
			//p = new Player("a", "b", "c", "d", "e");
			//Assert::AreEqual((std::string) "a b", p->printFullName());
			//Player a = { "A", "b", "c", "d", "e" };

		}
	};
}
